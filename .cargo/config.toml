# getrandom 0.3 requires an explicit backend selection on
# wasm32-unknown-unknown (in addition to its `wasm_js` cargo feature).
[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
